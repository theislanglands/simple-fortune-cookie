name: pipeline
on: push

jobs:
  Clone_down:
    name: Clone down repository
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echo "Hello World!"
      - name: container
        container: golang
        run: echo "container"