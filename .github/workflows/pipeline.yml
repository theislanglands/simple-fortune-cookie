name: pipeline
on: push

jobs:
  Clone-down:
    name: Clone down repository
    runs-on: ubuntu-latest
    container: golang
    steps:
    - uses: actions/checkout@v2
  
  Run test:
    runs-on: ubuntu-latest
    - name: Run test
      run: echo "Hello World!"
